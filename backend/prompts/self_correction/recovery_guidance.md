[SYSTEM INNER-VERIFICATION / ACTION RECOVERY (Attempt {attempt}/{max_retries})]:
{diag_line}
Autonomous Recovery Guidance: Tool '{tool_name}' encountered repeated issues ({err_type_or_error}). Do not repeat the exact same invocation without modification. Inspect the error output, verify file paths, arguments, and dependencies, or employ an alternative tool approach to fulfill the user's task.
